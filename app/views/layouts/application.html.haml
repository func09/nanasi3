!!!
%html
  %head
    %title= yield(:title) || "つぶやき＠名無し"
    = javascript_include_tag 'http://ajax.googleapis.com/ajax/libs/jquery/1.4.2/jquery.min.js'
    = yield(:head)
  %body(class="#{controller_name}-#{action_name}")
    #container
      
      #header
        %h1 つぶやき＠名無し
        #navSection
          %ul#nav
            %li= link_to 'ホーム', '/'
            - if logged_in?
              %li= link_to 'ログアウト', logout_path
            - else
              %li= link_to 'ログイン', login_path
        
      #main
        #content-section
          - flash.each do |name, msg|
            %div(class="flash #{name}" id="flash_#{name}")
              %p= msg
          = yield
        #side-section
          .adsense
            :javascript
               google_ad_client = "pub-7941320190288826";
               google_ad_slot = "6943591828";
               google_ad_width = 250;
               google_ad_height = 250;
            = javascript_include_tag 'http://pagead2.googlesyndication.com/pagead/show_ads.js'
          = yield(:side)
      
      #footer
        &copy;2010 func09 <a href='/about'>このサービスについて</a>
